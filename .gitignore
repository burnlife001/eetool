.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
build/
dist/
*.egg-info/
.eggs/
*.log
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/
# Handover / handoff docs are local to the working agent; do not commit.
交接.md
# ATK-Logic capture ships native .dll helpers under capture/lib/bin; keep them
# out of the repo so users install them separately (e.g. via install.ps1 or
# the upstream ATK-Logic installer).
src/ee_toolkit/capture/lib/bin/
# IDE settings bundled inside vendored atk-decoders runtime dir.
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
